server {
  listen 8088;
  # Disable upstream compression so sub_filter can rewrite HTML.
  proxy_set_header Accept-Encoding "";

  location / {
    proxy_pass http://127.0.0.1:5678;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_hide_header X-Frame-Options;
    proxy_hide_header Content-Security-Policy;
    add_header Content-Security-Policy "frame-ancestors 'self' http://187.127.137.136:8087 http://187.127.137.136:8088" always;

    # Hide n8n branding (logo) inside the embedded editor.
    sub_filter '</head>' '<style>#sidebar .logo,a[href="/home/workflows"] .logo,[data-test-id="n8n-logo"],.n8n-logo,header .logo{display:none!important}</style></head>';
    sub_filter_once on;
    sub_filter_types text/html;

    proxy_read_timeout 86400;
    proxy_send_timeout 86400;
  }
}
