{
  "name": "@fabric/chain-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Chain worker service — re-exports @fabric/evm-chain for backward compatibility.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@fabric/evm-chain": "workspace:*"
  }
}
